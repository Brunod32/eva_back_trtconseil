{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}

    {# Alert message after joboffer creation #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success text-center mt-5">
            {{ message }}
        </div>
    {% endfor %}

    <section class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mb-5 mt-3">Candidature</h1>
            </div>
        </div>
    </section>

    <section class="container">
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3">
            <p>Vous avez postulé à une annonce, il faut maintenant la validation d'un consultant !</p>
            <p>Attention Bruno, vérifier dans BDD si les changements sont bons !</p>
        </div>
    </section>
    
{% endblock %}
